from django.db import models
from django.conf import settings

# Currently, there are no specific models needed for games 
# as it seems the games functionality is entirely frontend-based
# If game scores or other data needs to be stored,
# we can add models here later.
